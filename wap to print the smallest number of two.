#include <stdio.h>
int main() {
// check smallest no. 
// if num1 is small --> 1
// if num1 is small --> 0
    int num1,num2;
    printf("enter first num: ");
    scanf("%d",&num1);
    printf("enter second num: ");
    scanf("%d",&num2);
    printf("%d",num1 < num2);
    return 0;
}
